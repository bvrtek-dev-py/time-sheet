version: '3'

services:
  db:
    container_name: time_sheet-db
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db
    env_file:
      - '../src/settings/.env'

volumes:
  mongodb-data:
